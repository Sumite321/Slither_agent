CREATE TABLE APP.PLAYERSNAKE (ID INTEGER NOT NULL, ALIVEFOR FLOAT, CONTROLLER_ID INTEGER, DATE VARCHAR(255), LENGTH INTEGER, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE APP.GAMESNAKE (ID INTEGER NOT NULL, LENGTH INTEGER, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
